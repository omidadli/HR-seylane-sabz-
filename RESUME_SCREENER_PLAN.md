# پلن جامع: اپ مستقل «غربالگر هوشمند رزومه» (جدا شده از HR-seylane-sabz)

> هدف: فقط بخش «جذب و استخدام / غربالگری رزومه» از اپ فعلی، تبدیل به یک اپ تک‌کاره،
> فوق‌ساده (قابل فهم برای یک بچه ۵ ساله) ولی با موتور تحلیل قوی و گزارش تمیز.
>
> تصمیم‌های تاییدشده مالک محصول (نسخه ۲):
> ۱. **بدون سقف تعداد رزومه** — هر چند تا فایل، موتور باید با صف‌بندی هندل کند.
> ۲. **کلید Gemini داخلی اپ** — هیچ فیلد/تنظیم کلیدی در UI وجود ندارد.
> ۳. **آستانه‌ها و شاخص‌ها داینامیک** — AI دپارتمان را از متن شغل تشخیص می‌دهد و
>    شاخص‌ها + آستانه‌های مناسب همان دپارتمان را خودش می‌سازد؛ خروجی برای کاربر
>    همچنان «جامع اما خیلی ساده و قابل فهم» می‌ماند.

---

## ۱. اپ فعلی دقیقاً چه کار می‌کند؟ (کالبدشکافی)

| بخش فعلی | فایل/محل | وضعیت در اپ جدید |
|---|---|---|
| استخراج متن رزومه سمت کلاینت (PDF با pdfjs، DOCX با mammoth، TXT، رد PDF اسکن‌شده و doc قدیمی) | `src/utils/resumeTextExtraction.ts` | ✅ نگه دار + قوی‌تر کن (دو ستونی، ZIP، صف نامحدود) |
| آپلود گروهی + پشتیبانی ZIP | `BulkUploadModal.tsx` | ✅ نگه دار، ساده‌تر، بدون سقف تعداد |
| ارزیابی Gemini با معیار وزنی، rigor، نقل‌قول شاهد، قوت/ضعف، خلاصه مدیریتی | `server/gemini.ts` → `evaluateCandidateWithCriteria` | ✅ نگه دار ولی معیارها + آستانه‌ها «خودکار و دپارتمان‌محور» ساخته شوند |
| دسته‌بندی ۳ سطحی ثابت (اولویت مصاحبه ۷+ / نیازمند بررسی ۵-۷ / رد اولیه زیر ۵) | `server/gemini.ts` + `BulkScreeningStudio.tsx` | 🔄 داینامیک شود: آستانه هر شغل را AI بر اساس دپارتمان/حساسیت نقش تعیین کند |
| کارت‌های نتایج + KPI + خروجی CSV | `BulkScreeningStudio.tsx` | ✅ نگه دار ولی مینیمال (بدون کانبان، بدون اکشن‌های استخدامی) |
| کانبان، مراحل استخدام، استخر استعداد، ایمیل، تقویم، چت ایجنت، رادار، RBAC/لاگین، وزن‌دهی دستی | کل ماژول recruitment | ❌ حذف کامل |

**درس مهم از اپ فعلی که باید حفظ شود:** صداقت موتور — اگر کلید Gemini نبود، اپ فعلی وانمود به هوش مصنوعی
نمی‌کند. اپ جدید هم: **هرگز امتیاز ساختگی** (با این تفاوت که کلید داخلی است و کاربر درگیر آن نمی‌شود).

---

## ۲. فلسفه محصول: «سادگی بیرون، قدرت پنهان»

- **تست بچه ۵ ساله:** هر صفحه فقط «یک کار» دارد. بدون منو، بدون لاگین، بدون تنظیمات، بدون فیلد کلید.
- **۳ صفحه، نه بیشتر:** ۱) شروع → ۲) پردازش → ۳) نتایج.
- **زبان محاوره‌ای ساده:** «این شغل چیه و چی می‌خوای؟» به‌جای «شرح شغل / الزامات».
- **هوش نامرئی:** کاربر فقط متن آزاد می‌نویسد، ولی AI در پشت صحنه دپارتمان را تشخیص می‌دهد،
  شاخص‌های مخصوص همان دپارتمان + آستانه‌های مناسب را می‌سازد و با آن‌ها ارزیابی می‌کند.
- **مقیاس نامحدود:** چه ۵ رزومه چه ۵۰۰ رزومه — موتور با صف و پردازش بچ‌به‌بچ جلو می‌رود و
  نوار پیشرفت واقعی نشان می‌دهد.

---

## ۳. معماری پیشنهادی (۳ صفحه)

### صفحه ۱ — شروع (تنها صفحه ورودی)
1. باکس متنی بزرگ: «این شغل چیه و چی می‌خوای؟» (متن آزاد فارسی، حتی ۲ خط کافی است)
2. ناحیه آپلود چندتایی: درگ‌اندراپ + دکمه «انتخاب رزومه‌ها» (PDF، Word، ZIP، TXT) — **بدون سقف تعداد**،
   با پیام تشویقی ساده: «هر چند تا رزومه داری، همین‌جا بریز 📥»
3. دکمه بزرگ «شروع بررسی» (تا شرح شغل + حداقل ۱ رزومه معتبر نباشد، غیرفعال با راهنمای ساده)
4. نوار ۳ مرحله‌ای بالای صفحه: ۱. شغل و رزومه‌ها ← ۲. بررسی ← ۳. نتایج
5. **هیچ فیلد کلیدی، هیچ تنظیماتی.** کلید Gemini داخلی اپ است.

### صفحه ۲ — پردازش
- لودینگ ساده + پیام‌های فارسی چرخشی («داریم رزومه‌ها رو می‌خونیم…»، «داریم با شغل مقایسه می‌کنیم…»)
- **نوار پیشرفت واقعی** («۷ از ۲۰۰ رزومه بررسی شد» + درصد) — چون تعداد نامحدود است، این نوار حیاتی است
- نمایش ساده وضعیت: ✅ بررسی‌شده / ⏳ در صف / ⚠️ مشکل‌دار (با عدد فارسی)
- دکمه «انصراف» که به صفحه شروع برمی‌گرداند

### صفحه ۳ — نتایج
- خلاصه مدیریتی یک‌خطی («از ۲۰۰ رزومه، ۱۲ نفر برای مصاحبه پیشنهاد می‌شن 🎯»)
- باکس دوستانه «هوش مصنوعی این شغل رو این‌طوری فهمید 🤖»: یک‌دو جمله ساده، مثلاً
  «این یه شغل فروشه؛ پس بیشتر از همه به فن بیان و سابقه فروش امتیاز دادم و چون سمت حساسیه، سخت‌گیر بودم.»
  (این باکس، «تحلیل جامع اما ساده» است — کاربر می‌فهمد AI چه کرده، بدون دیدن هیچ عدد و وزن پیچیده‌ای)
- ۳ عدد درشت: ✅ مصاحبه شود / 🟡 بررسی بیشتر / 🔴 رد شود
- لیست کارت‌ها مرتب از بیشترین امتیاز: نام + امتیاز ۰-۱۰۰ رنگی + خلاصه ۲-۳ خطی «چرا این امتیاز؟»
  + نقاط قوت (با شاهد متنی) + کمبودها + برچسب توصیه نهایی
- بخش خاکستری «قابل قضاوت نیست 🤷» (فایل خراب/اسکن‌شده، متن ناکافی، کاملاً نامرتبط — هرکدام با یک جمله دلیل)
- ۲ دکمه: «دانلود گزارش 📥» (اکسل فارسی) + «شروع دوباره 🔄» + صفحه Print-friendly برای PDF

---

## ۴. موتور تحلیل: قوی بدون پیچیدگی (قلب تپنده)

### پایپ‌لاین (نامرئی برای کاربر)

**مرحله ۱ — استخراج متن نامحدود با صف**
- PDF (تک‌ستونی و دوستونی)، DOCX، TXT، فایل‌های داخل ZIP — **بدون سقف تعداد**
- صف پردازش: استخراج و ارزیابی در بچ‌های موازی ۳-۵ تایی، بچ‌به‌بچ تا آخر صف
- PDF اسکن‌شده / doc قدیمی / فرمت نامعتبر / متن کمتر از ~۵۰ کاراکتر → «قابل قضاوت نیست» با دلیل ساده
- فایل‌های تکراری (هم‌نام یا هم‌محتوا) هوشمندانه یک‌بار حساب شوند، با اطلاع ساده

**مرحله ۲ — نرمال‌سازی فارسی**
- یکدست‌سازی نیم‌فاصله، تبدیل ي/ك عربی به ی/ک فارسی، تبدیل ارقام فارسی/عربی به انگلیسی (فقط تحلیل داخلی)

**مرحله ۳ — تحلیل دپارتمان‌محور + ارزیابی (با Gemini)**
- **پاس صفر — «فهمیدن شغل» (یک‌بار برای کل بچ، خودکار):**
  AI از همان متن آزاد: (الف) دپارتمان/حوزه را تشخیص می‌دهد (فروش، فنی/مهندسی، مالی،
  تولید/کارخانه، منابع انسانی، مارکتینگ، پشتیبانی، مدیریت و...)، (ب) سطح ارشدیت و حساسیت نقش
  را حدس می‌زند، (ج) شاخص‌های مخصوص همان دپارتمان + وزن‌ها را می‌سازد، (د) **آستانه‌های
  داینامیک** همان شغل را تعیین می‌کند (با گاردریل: آستانه مصاحبه بین ۷۰-۸۵، آستانه بررسی بین ۴۵-۶۰)،
  (هـ) یک توضیح ۱-۲ جمله‌ای خیلی ساده برای نمایش در باکس «AI این‌طوری فهمید» تولید می‌کند.
  مثال: فروش → وزن بالا برای فن بیان/مذاکره و سابقه فروش؛ فنی → مهارت‌ها و پروژه‌ها؛
  مالی → دقت و مدارک؛ نقش مدیریتی/حساس → آستانه سخت‌گیرانه‌تر.
- **پاس یک — ارزیابی فردی هر رزومه (استدلال گام‌به‌گام):** استخراج نکات کلیدی رزومه
  (سابقه، مهارت، تحصیلات، پروژه‌ها/دستاوردها) → مقایسه با شاخص‌های همان دپارتمان → امتیاز ۰-۱۰۰
  + دلیل + شاهد متنی. قوانین سقفی ضد بادکردن نمره: مهارت الزامی اصلی غایب = سقف ۴۵؛
  کاملاً نامرتبط = رد صادقانه؛ اطلاعات ناکافی = پرچم جدا بدون نمره‌سازی.
- **پاس دو — کالیبراسیون مقیاس‌پذیر:** چون تعداد نامحدود است، مقایسه همه‌به‌همه ممکن نیست؛
  پس: مرتب‌سازی سراسری با نمره‌های مطلق + یک پاس کالیبراسیون AI فقط روی «نفرات برتر (مثلاً ۱۵ نفر اول)
  + موارد لب‌مرزی (‎±۵ نمره حول آستانه‌ها)» با تعدیل حداکثر ‎±۱۰. بقیه با نمره مطلق می‌مانند.
  نتیجه: رتبه‌بندی معنادار در هر مقیاسی، بدون انفجار هزینه/زمان.

**مرحله ۴ — خروجی ساخت‌یافته + گزارش**
- JSON ثابت برای هر کاندید (با پارس مقاوم) + JSON تحلیل شغل (دپارتمان، آستانه‌ها، شاخص‌ها، توضیح ساده)
- گزارش اکسل راست‌به‌چپ: شیت اول خلاصه شغل (دپارتمان تشخیص‌داده‌شده + آستانه‌ها + توضیح ساده)،
  شیت دوم جدول رتبه‌بندی کامل همه کاندیدها

### ضد توهم (خط قرمز)
- هر نقطه قوت/ضعف = شاهد متنی (نقل‌قول کوتاه واقعی از رزومه)؛ بدون شاهد، ادعا ممنوع
- نام کاندید فقط اگر واقعاً در متن بود؛ وگرنه «کاندید N» + نام فایل
- عناوین شغلی/مدارک/اصطلاحات بازار کار ایران، فارسی یا معادل انگلیسی رایج، درست تشخیص داده شود

### تاب‌آوری برای مقیاس نامحدود
- هم‌زمانی محدود (۳-۵ درخواست موازی) + بک‌آف نمایی برای خطای 429 + تلاش مجدد ۲-۳ بار
- خرابی یک رزومه بقیه را متوقف نکند («بررسی نشد، دوباره تلاش کن» با دلیل)
- اگر کلید داخلی missing/invalid بود → پیام شفاف فارسی و توقف صادقانه؛ **هرگز امتیاز فیک**
- انصراف کاربر در هر لحظه بدون خراب شدن حالت اپ

---

## ۵. مدل داده (ساده)

```ts
interface JobAnalysis {
  department: string;            // مثلاً «فروش» — تشخیص AI
  seniority: string;             // مثلاً «کارشناس» / «مدیر»
  thresholds: { interview: number; review: number };  // داینامیک، با گاردریل
  criteria: { title: string; weight: number; mustHave: boolean; keywords: string[] }[];
  plainExplanation: string;      // ۱-۲ جمله ساده برای باکس «AI این‌طوری فهمید»
}
interface JobInput { rawText: string; analysis: JobAnalysis }
interface CandidateResult {
  id: string; fileName: string; candidateName: string | null;
  score: number; summary: string;
  strengths: { point: string; evidence: string }[];
  weaknesses: { point: string; evidence: string }[];
  recommendation: 'INTERVIEW' | 'REVIEW' | 'REJECT';
  insufficientInfo: boolean; irrelevant: boolean; failedReason?: string;
}
interface ScreeningReport { job: JobInput; results: CandidateResult[]; createdAtJalali: string; stats: {...} }
```

---

## ۶. تکنولوژی پیشنهادی (سازگار با Google AI Studio)
- React + Vite + Tailwind، فونت وزیرمتن، کاملاً RTL
- `@google/genai` برای Gemini (مدل: `gemini-2.5-flash`) — کلید **داخلی** از متغیر محیطی، بدون هیچ UI کلید
- `pdfjs-dist` + `mammoth` + `jszip` برای استخراج متن (کد آماده در ریپوی فعلی هست)
- `xlsx` (SheetJS) برای خروجی اکسل فارسی دو‌شیته
- صف پردازش سمت کلاینت با هم‌زمانی محدود + بک‌آف

## ۷. چیزهایی که عمداً اضافه نمی‌شوند (لیست ممنوعه)
لاگین/عضویت، منوی ناوبری، کانبان، مراحل استخدام، استخر استعداد، ایمیل، تقویم،
وزن‌دهی دستی، **فیلد کلید API**، سقف تعداد فایل، چت‌بات، نمودار رادار، اتصال خارجی، داشبورد.

## ۸. معیار پذیرش (تست «بچه ۵ ساله» + تست کیفیت)
1. کاربر غیرفنی بدون آموزش، در کمتر از ۱ دقیقه بررسی را شروع کند
2. بچ ۱۰۰+ رزومه بدون کرش/توقف کامل شود و نوار پیشرفت واقعی جلو برود
3. ۴ شغل از دپارتمان‌های مختلف (فروش، فنی، مالی، تولید) → دپارتمان + شاخص‌های متفاوت و معقول برای هرکدام
4. بین رزومه‌ها (دوستونی، نامرتبط، ناقص، اسکن‌شده) هر ۴ حالت خاص درست هندل شوند
5. هیچ ادعایی بدون شاهد متنی نباشد؛ هیچ امتیاز فیکی در خطای API تولید نشود
6. گزارش اکسل دو‌شیته (خلاصه شغل + رتبه‌بندی) فارسی و مرتب باز شود

---

## ۹. پرامپت نهایی آماده برای Google AI Studio (کپی کن و بچسبان)

> نکته: اول ریپو را در AI Studio ایمپورت/آپلود کن (یا فایل‌های کلیدی را بده)، بعد این پرامپت را بده.

---

```text
نقش تو: یک تیم محصول و مهندسی ارشد فول‌استک (متخصص React، هوش مصنوعی و زبان فارسی) هستی
که قرار است یک «اپلیکیشن مستقل غربالگری هوشمند رزومه» بسازی.

زمینه: من یک ریپوی گیت‌هاب دارم (HR-seylane-sabz / سامانه «کارا») که یک ماژول «جذب و استخدام /
غربالگری رزومه» دارد. منطق‌های قابل استفاده مجدد آن:
- استخراج متن رزومه سمت کلاینت با pdfjs-dist و mammoth در فایل src/utils/resumeTextExtraction.ts
- ارزیابی رزومه با Gemini در تابع evaluateCandidateWithCriteria داخل server/gemini.ts
  (امتیازدهی وزنی، نقاط قوت/ضعف، نقل‌قول شاهد از رزومه، خلاصه مدیریتی، دسته‌بندی ۳ سطحی)
- نمایش نتایج و خروجی CSV در src/components/recruitment/BulkScreeningStudio.tsx
از این منطق‌ها «ایده و الگو» بگیر ولی یک اپ جدید، مستقل و بسیار ساده‌تر بساز. کیفیت و دقت موتور
تحلیل را حفظ کن و حتی بهترش کن، ولی همه پیچیدگی‌های محصول (کانبان، مراحل، لاگین، ایمیل و...)
را حذف کن.

هدف محصول: یک ابزار تک‌کاره «غربالگری اولیه رزومه» که مخاطبش افراد کاملاً غیرفنی هستند.
قانون طلایی: یک بچه ۵ ساله باید بفهمد این اپ چیه و چطور باهاش کار کنه. هر چیزی که این قانون را
می‌شکند حذف شود.

═══════════════════ قوانین سفت‌وسخت (نقض‌ناپذیر) ═══════════════════
۱. فقط و فقط ۳ صفحه: «شروع» ← «پردازش» ← «نتایج». هیچ صفحه چهارمی نساز.
۲. ممنوع: لاگین/عضویت، منوی ناوبری، کانبان، مراحل استخدام، استخر استعداد، ایمیل، تقویم،
   وزن‌دهی دستی، فیلد یا صفحه کلید API، چت‌بات، نمودار، داشبورد، اتصال به سرویس خارجی،
   صفحه تنظیمات جدا. کلید Gemini داخلی اپ است و از متغیر محیطی خوانده می‌شود.
۳. بدون سقف تعداد رزومه: موتور باید با صف‌بندی و بچ‌بندی، هر تعدادی فایل (۵ تا یا ۵۰۰ تا)
   را کامل پردازش کند. هیچ محدودیت تعدادی در UI نگذار.
۴. آستانه‌های قبولی/ردی ثابت و سراسری ممنوع: آستانه هر شغل را AI به‌صورت داینامیک بر اساس
   دپارتمان و حساسیت نقش تعیین می‌کند (با گاردریل پایین‌تر).
۵. کاملاً راست‌به‌چپ (RTL) و فارسی، با فونت وزیرمتن (Vazirmatn). همه اعداد نمایشی فارسی.
۶. هرگز و تحت هیچ شرایطی امتیاز یا تحلیل ساختگی/فیک تولید نکن. اگر متنی استخراج نشد،
   اگر API خطا داد، یا اگر اطلاعات کافی نبود، صادقانه اعلام کن (جزئیات پایین‌تر).
۷. هر نقطه قوت و ضعفی که نمایش می‌دهی باید یک «شاهد متنی» (نقل‌قول کوتاه واقعی از متن
   رزومه) داشته باشد. بدون شاهد، ادعا ممنوع.
۸. نام کاندید را فقط اگر واقعاً در متن رزومه بود استخراج کن؛ وگرنه «کاندید ۱، ۲، ...»
   + نام فایل را نمایش بده. هرگز اسم حدس نزن.

═══════════════════ صفحه ۱ — شروع (تنها ورودی) ═══════════════════
- بالای صفحه: نوار ۳ مرحله‌ای ساده (۱. شغل و رزومه‌ها ← ۲. بررسی ← ۳. نتایج).
- یک تیتر خیلی ساده مثل «کدوم رزومه‌ها به این شغل می‌خورن؟» + یک جمله راهنما.
- باکس متنی بزرگ چندخطی با placeholder دقیقاً: «این شغل چیه و چی می‌خوای؟»
  (متن آزاد فارسی؛ حتی ۲-۳ خط هم قبول است؛ هیچ فرم جدا برای عنوان شغل/مهارت/سابقه نساز).
- ناحیه آپلود چندتایی رزومه: هم drag & drop هم دکمه «انتخاب رزومه‌ها».
  فرمت‌ها: PDF و Word (docx) و فایل ZIP (حاوی چند رزومه) و txt.
  پیام تشویقی ساده: «هر چند تا رزومه داری، همین‌جا بریز 📥» — بدون هیچ سقفی.
  هر فایل اضافه شد، اسمش با تیک سبز در لیست «رزومه‌های آماده» نمایش داده شود و قابل حذف (×) باشد.
  فایل تکراری هوشمندانه یک‌بار حساب شود.
- یک دکمه بزرگ و واضح: «شروع بررسی ✅». تا وقتی (الف) متن شغل خالی است یا
  (ب) هیچ رزومه معتبری اضافه نشده، دکمه غیرفعال باشد و زیرش با زبان ساده بگوید چرا
  (مثلاً: «اول بگو شغل چیه 👆» یا «هنوز رزومه‌ای اضافه نکردی 👆»).
- هیچ فیلد کلید، هیچ تنظیمات، هیچ چیز اضافه دیگری در این صفحه نباشد.

═══════════════════ صفحه ۲ — پردازش ═══════════════════
- لودینگ ساده و دوستانه با پیام‌های فارسی چرخشی، مثلاً:
  «داریم رزومه‌ها رو می‌خونیم… 📖» / «داریم با شغل مقایسه می‌کنیم… ⚖️» /
  «داریم بهترین‌ها رو انتخاب می‌کنیم… 🏆»
- نوار پیشرفت واقعی و صادقانه: «۷ از ۲۰۰ رزومه بررسی شد» + درصد. انیمیشن فیک ممنوع.
- سه عدد وضعیت ساده: ✅ بررسی‌شده / ⏳ در صف / ⚠️ مشکل‌دار.
- دکمه متنی «انصراف و برگشت» که به صفحه شروع برمی‌گرداند.
- خطاهای تکی (یک رزومه خراب یا یک خطای API) کل فرایند را متوقف نکند؛ آخر کار گزارش شود.

═══════════════════ صفحه ۳ — نتایج ═══════════════════
- خلاصه مدیریتی یک‌خطی در بالا، مثلاً: «از ۲۰۰ رزومه، ۱۲ نفر برای مصاحبه پیشنهاد می‌شن 🎯»
- باکس دوستانه «هوش مصنوعی این شغل رو این‌طوری فهمید 🤖»: دقیقاً ۱-۲ جمله خیلی ساده
  که AI تولید کرده، مثلاً: «این یه شغل فروشه؛ پس بیشتر از همه به فن بیان و سابقه فروش امتیاز دادم
  و چون سمت حساسیه، سخت‌گیرانه بررسی کردم.» همین باکس، «تحلیل جامع اما ساده» است —
  هیچ عدد، وزن، آستانه یا اصطلاح فنی در آن نباشد.
- ۳ عدد درشت رنگی: ✅ «مصاحبه شود» / 🟡 «بررسی بیشتر» / 🔴 «رد شود».
- کارت‌های کاندیدها مرتب از بیشترین امتیاز به کمترین. هر کارت:
  ① نام کاندید (یا «کاندید N» + نام فایل) ② امتیاز ۰ تا ۱۰۰، درشت، با رنگ
  (سبز = بالای آستانه مصاحبه، زرد = بین دو آستانه، قرمز = زیر آستانه بررسی؛ آستانه‌ها همان
  مقادیر داینامیکی‌اند که AI برای این شغل تعیین کرده) ③ خلاصه ۲-۳ خطی «چرا این امتیاز؟»
  به زبان ساده ④ لیست «نقاط قوت ✅» (حداکثر ۴ مورد، هر کدام + شاهد متنی کوتاه)
  ⑤ لیست «کمبودها ⚠️» (حداکثر ۴ مورد، هر کدام + شاهد/دلیل) ⑥ برچسب توصیه نهایی.
- بخش جدای خاکستری در انتهای لیست: «قابل قضاوت نیست 🤷» برای موارد: فایل خراب/اسکن‌شده،
  متن ناکافی، یا رزومه کاملاً نامرتبط — هر کدام با یک جمله دلیل ساده.
- دکمه اصلی: «دانلود گزارش 📥» (فایل اکسل .xlsx فارسی و راست‌به‌چپ، دو شیت:
  شیت ۱ «خلاصه شغل»: دپارتمان تشخیص‌داده‌شده + توضیح ساده + آستانه‌های این شغل + آمار؛
  شیت ۲ «رتبه‌بندی»: رتبه | نام | نام فایل | امتیاز | توصیه | خلاصه | نقاط قوت | کمبودها).
- دکمه دوم: «شروع دوباره 🔄» (برگشت به صفحه ۱ با پاک شدن نتایج قبلی).
- صفحه نتایج باید Print-friendly باشد (با CSS چاپ تمیز بتوان PDF گرفت).

═══════════════════ موتور تحلیل (بخش حیاتی — دقت حداکثری) ═══════════════════
رزومه‌ها و شرح شغل عمدتاً فارسی‌اند. پایپ‌لاین (نامرئی برای کاربر):

مرحله ۱ — استخراج متن نامحدود با صف: با pdfjs-dist (مرتب‌سازی بر اساس مختصات برای رزومه‌های
دوستونی) و mammoth برای docx و باز کردن ZIP با jszip. متن خالی/کمتر از ~۵۰ کاراکتر = نامعتبر.
PDF اسکن‌شده، doc قدیمی، و فرمت نامعتبر → بدون امتیاز فیک، به بخش «قابل قضاوت نیست» برود
با دلیل ساده فارسی. پردازش در بچ‌های موازی ۳-۵ تایی، بچ‌به‌بچ تا پایان صف، با شمارش دقیق
برای نوار پیشرفت واقعی.

مرحله ۲ — نرمال‌سازی فارسی: یکدست‌سازی نیم‌فاصله، تبدیل ي/ك عربی به ی/ک فارسی،
تبدیل ارقام فارسی/عربی به انگلیسی (فقط برای تحلیل داخلی؛ نمایش همچنان فارسی).

مرحله ۳ — تحلیل دپارتمان‌محور + ارزیابی با Gemini (مدل gemini-2.5-flash):
  پاس صفر — «فهمیدن شغل» (فقط یک‌بار برای کل بچ، کاملاً خودکار؛ کاربر هیچ فرمی نمی‌بیند):
  از همان متن آزاد شغل استخراج کن: (الف) دپارتمان/حوزه (فروش، فنی/مهندسی، مالی، تولید،
  منابع انسانی، مارکتینگ، پشتیبانی، مدیریت و...)، (ب) سطح ارشدیت و حساسیت نقش،
  (ج) شاخص‌های ارزیابی مخصوص همان دپارتمان با وزن (جمع=۱۰۰). مثال منطق: فروش → وزن بالا
  برای فن بیان/مذاکره و سابقه فروش؛ فنی → مهارت‌های تخصصی و پروژه‌ها؛ مالی → دقت و مدارک؛
  تولید → سابقه عملی و ایمنی؛ نقش مدیریتی/حساس → سخت‌گیری بیشتر.
  (د) آستانه‌های داینامیک همان شغل با این گاردریل: آستانه «مصاحبه شود» بین ۷۰ تا ۸۵،
  آستانه «بررسی بیشتر» بین ۴۵ تا ۶۰ (نقش‌های حساس/ارشد به سقف بازه نزدیک‌تر،
  نقش‌های حجمی/عمومی به کف بازه).
  (هـ) یک توضیح ۱-۲ جمله‌ای خیلی ساده و غیرفنی (plainExplanation) برای نمایش در باکس
  «AI این‌طوری فهمید» — بدون عدد و اصطلاح تخصصی.
  خروجی JSON پاس صفر:
  {"department":"...","seniority":"...","thresholds":{"interview":75,"review":50},
   "criteria":[{"title":"...","weight":40,"mustHave":true,"keywords":["..."]}],
   "plainExplanation":"..."}
  اگر متن شغل خیلی کوتاه/مبهم بود، حدس معقول بزن و ادامه بده (متوقف نشو).
  پاس یک — ارزیابی فردی هر رزومه با «استدلال گام‌به‌گام»: اول نکات کلیدی رزومه را استخراج کن
  (سابقه، مهارت‌ها، تحصیلات، پروژه‌ها/دستاوردها)، بعد با شاخص‌های همان دپارتمان مقایسه کن،
  بعد امتیاز ۰-۱۰۰ بده. قوانین سقفی ضد بادکردن نمره: اگر مهارت اصلی الزامی غایب است سقف ۴۵؛
  اگر رزومه کاملاً نامرتبط است توصیه REJECT با توضیح صادقانه؛ اگر اطلاعات برای قضاوت کافی
  نیست پرچم insufficientInfo=true و بدون نمره‌سازی.
  عناوین شغلی، مدارک دانشگاهی و اصطلاحات رایج بازار کار ایران را چه فارسی چه معادل انگلیسی
  رایج (مثل کارشناسی/لیسانس/Bachelor) درست تشخیص بده.
  پاس دو — کالیبراسیون مقیاس‌پذیر (چون تعداد نامحدود است، مقایسه همه‌به‌همه ممنوع):
  مرتب‌سازی سراسری با نمره‌های مطلق، سپس یک پاس کالیبراسیون AI فقط روی «۱۵ نفر اول +
  موارد لب‌مرزی (±۵ نمره حول هر آستانه)» با تعدیل حداکثر ‎±۱۰. بقیه با نمره مطلق می‌مانند.

مرحله ۴ — خروجی ساخت‌یافته: خروجی مدل برای هر کاندید همیشه دقیقاً این JSON باشد
(با پارس مقاوم: حذف کدفنس markdown، اصلاح کاراکترهای کنترلی، تلاش مجدد در صورت خرابی):
{
  "candidateName": "string یا null",
  "score": 0-100,
  "summary": "خلاصه ۲-۳ جمله‌ای فارسیِ دلیل امتیاز",
  "strengths": [{"point": "...", "evidence": "نقل‌قول کوتاه واقعی از رزومه"}],
  "weaknesses": [{"point": "...", "evidence": "..."}],
  "recommendation": "INTERVIEW | REVIEW | REJECT",
  "insufficientInfo": false,
  "irrelevant": false
}
نگاشت توصیه با آستانه‌های داینامیک همان شغل: بالای آستانه مصاحبه = INTERVIEW
(«مصاحبه شود ✅»)، بین دو آستانه = REVIEW («بررسی بیشتر 🟡»)، زیر آستانه بررسی = REJECT
(«رد شود 🔴»).

تاب‌آوری (حیاتی برای تعداد نامحدود): هم‌زمانی حداکثر ۳-۵ درخواست؛ بک‌آف نمایی برای خطای
429 و سهمیه؛ تلاش مجدد تا ۳ بار برای هر رزومه؛ خرابی یک رزومه بقیه را متوقف نکند (آن کارت
«بررسی نشد، دوباره تلاش کن» شود)؛ اگر کلید داخلی در دسترس/معتبر نبود، شروع بررسی با پیام
شفاف فارسی متوقف شود و هیچ امتیازی تولید نشود.

═══════════════════ تکنولوژی و ساختار ═══════════════════
React + Vite + Tailwind + فونت وزیرمتن. کتابخانه‌ها: @google/genai ،pdfjs-dist ،mammoth ،
jszip ،xlsx. کلید از متغیر محیطی (بدون هیچ UI برای کلید). ساختار پیشنهادی: src/App.tsx
(مدیریت ۳ صفحه) + components/StartScreen ،ProcessingScreen ،ResultsScreen ،ScoreCard ،
JobInsightBox + lib/extractText.ts ،normalizeFa.ts ،gemini.ts (پاس صفر/یک/دو + صف و بک‌آف) ،
report.ts + types.ts. موبایل‌فرندلی و تمیز.

═══════════════════ نحوه تحویل (دو گام) ═══════════════════
گام ۱: اول فقط «نقشه راه و ساختار» را نشان بده: لیست فایل‌ها و کامپوننت‌ها، مدل داده،
فلو ۳ صفحه، شمای JSON پاس صفر و خروجی مدل، و طراحی صف پردازش نامحدود — و از من تایید بگیر.
گام ۲: بعد از تایید من، کد کامل و آماده‌اجرا را بنویس.
در پایان هم ۶ سناریوی تست پذیرش را چک کن: ۱) کاربر غیرفنی در ۱ دقیقه شروع کند
۲) بچ ۱۰۰+ رزومه کامل و بدون توقف پردازش شود ۳) برای ۴ دپارتمان مختلف، شاخص و آستانه
متفاوت و معقول تولید شود ۴) هر ۴ حالت خاص (دوستونی، نامرتبط، ناقص، اسکن‌شده) درست هندل شود
۵) هیچ ادعایی بدون شاهد متنی نباشد و با خطای API هیچ امتیاز فیکی ساخته نشود
۶) گزارش اکسل دوشیته فارسی و مرتب باز شود.
```

---

## ۱۰. قدم‌های بعدی (پیشنهاد مسیر)
1. [x] تایید تصمیم‌های کلیدی: بدون سقف / کلید داخلی / آستانه داینامیک دپارتمان‌محور
2. [ ] گرفتن استراتژی تکمیلی از Claude و سوار کردن روی همین پلن
3. [ ] دادن پرامپت نهایی به Google AI Studio + گرفتن «گام ۱» (ساختار)
4. [ ] تایید ساختار → گرفتن کد کامل → تست با رزومه‌های واقعی فارسی از چند دپارتمان
