# گزارش کامل باگ‌های فرانت‌اند و بک‌اند — سامانه HR سیلانه سبز

**تاریخ ممیزی:** ۱۴۰۵/۰۶/۱۸ (۲۰۲۶-۰۹-۰۹)
**برنچ:** `arena/01a084ce-hr-seylane-sabz` · **کامیت مبنا:** `1817496`
**روش:** مرور کامل کد (server.ts، سرویس‌های سرور، store، تمام کامپوننت‌های src) + اجرای زنده سرور + اجرای هر دو سوئیت تست موجود (`product_fix_verification.cjs` با ۴۷ آزمون و `strict_e2e_test.cjs` با ۷ آزمون).

> **نکتهٔ مهم پیش‌زمینه:** گزارش قدیمی `PRODUCT_BUG_REPORT.md` متعلق به کامیت قدیمی‌تر (`56ac3fd`) است. بخش بزرگی از باگ‌های Critical/High آن (مالیات دوبرابر، نبود ماندهٔ مرخصی، نشت PII، تأیید انبوه مرخصی، امتیاز تصادفی بارگذاری گروهی، …) **قبلاً اصلاح شده** و هر ۴۷ آزمون رگرسیون الان سبز است. لیست زیر فقط **باکس‌های باقی‌مانده و تازه‌ای** است که در کد فعلی وجود دارد و باید برطرف شوند.

---

## خلاصهٔ اجرایی

| شدت | تعداد |
|---|---|
| بالا (High) | ۴ |
| متوسط (Medium) | ۵ |
| پایین (Low) | ۴ |

سه دستهٔ اصلی مشکل:
1. **داده‌های جعلی/تصادفی که به‌صورت «محاسبه‌شده» نمایش داده می‌شوند** (بک‌اند و فرانت).
2. **ناهماهنگی قرارداد فرانت/بک** — ماژول «حاکمیت هوش مصنوعی» سه endpoint فراخوانی می‌کند که در بک‌اند وجود ندارند و از مسیر Command Palette قابل دسترسی است.
3. **باگ‌های همگام‌سازی state و UI فرانت** (state محلی از App جدا می‌شود، نوار پیشرفت جعلی، سال پیش‌فرض حقوق، تاریخ‌های hardcoded).

---

# بخش ۱ — باگ‌های بک‌اند

## B1 · بالا · ارزیابی ویدیویی HireVue نمرات تصادفی و توصیهٔ جعلی تولید می‌کند
**فایل:** `server.ts` — خطوط `737` تا `739`
**Endpoint:** `POST /api/competitor/hirevue/evaluate-submission`

```ts
overallScore: Math.floor(82 + Math.random() * 16),
confidenceScore: Math.floor(80 + Math.random() * 18),
clarityScore: Math.floor(85 + Math.random() * 14),
fairnessAuditScore: 99,
aiRecommendation: 'STRONG_RECOMMEND' as const,
```

**شرح:** صرف‌نظر از محتوای `simulatedTranscript`، نمرات به‌صورت تصادفی در بازهٔ ۸۲ تا ۹۸ تولید می‌شود و `aiRecommendation` همیشه `STRONG_RECOMMEND` و `summaryInsight` یک متن ثابت ازپیش‌نوشته است. یعنی خروجی یک «ارزیابی هوشمند» ساختگی است که در UI به‌عنوان نتیجهٔ واقعی تحلیل ویدیو نمایش داده می‌شود.

**اثر:** تصمیم‌گیری استخدامی بر مبنای عدد تصادفی؛ نقض شفافیت ادعاشدهٔ محصول («وانمود به هوش مصنوعی نمی‌کنیم»).

**اصلاح پیشنهادی:** یا (الف) خروجی را صادقانه با برچسب `source: 'simulation'` و `aiAvailable: false` برگردانید، یا (ب) اگر قرار است واقعی باشد، `simulatedTranscript` را به Gemini بفرستید و نمرهٔ واقعی استخراج کنید و در نبود کلید صادقانه «در دسترس نیست» اعلام کنید.

---

## B2 · متوسط · افزایش تصادفی Impressions در سندیکیشن ZipRecruiter
**فایل:** `server.ts` — خط `797`
**Endpoint:** `POST /api/competitor/ziprecruiter/toggle-syndication`

```ts
if (status === 'ACTIVE') {
  channel.impressionsCount += Math.floor(100 + Math.random() * 300);
}
```

**شرح:** با هر بار فعال‌سازی کانال، تعداد بازدید (`impressionsCount`) یک عدد تصادفی بین ۱۰۰ تا ۴۰۰ اضافه می‌شود. دادهٔ جعلی که در گزارش‌های «سندیکیشن» به‌عنوان عدد واقعی نمایش داده می‌شود.

**اصلاح پیشنهادی:** حذف افزایش تصادفی؛ یا نگه‌داشتن عدد ثابت/صفر تا زمان اتصال واقعی API، همراه با برچسب صادقانهٔ «شبیه‌سازی».

---

## B3 · متوسط · دو متریک تحلیلی به‌صورت ثابت از seed برگردانده می‌شود ولی `computed: true` گزارش می‌شود
**فایل:** `server.ts` — خطوط `1728` تا `1729` (و `1737`)
**Endpoint:** `GET /api/analytics/metrics`

```ts
averageTimeToHireDays: isHRViewer ? dbStore.metrics.averageTimeToHireDays : null, // = 16 (ثابت seed)
costPerHireToman: isHRViewer ? dbStore.metrics.costPerHireToman : null,           // = 6,800,000 (ثابت seed)
...
computed: true,
```

**شرح:** برخلاف `activeHeadcount`، `hiredCandidatesCount` و `monthlyPayrollTotalToman` که واقعاً محاسبه می‌شوند، دو متریک `averageTimeToHireDays` و `costPerHireToman` مستقیماً از مقادیر ثابت seed در `store.ts` (۱۶ روز و ۶.۸ میلیون تومان) خوانده می‌شوند، در حالی که پاسخ `computed: true` دارد. این ادعای نادرستِ «محاسبه از دادهٔ زنده» است.

**اثر:** کارت‌های KPI داشبورد و گزارش تحلیلی اعدادی ساختگی را به‌عنوان عدد محاسبه‌شده نشان می‌دهند.

**اصلاح پیشنهادی:** این دو مقدار را واقعاً از دادهٔ زنده محاسبه کنید (مثلاً میانگین فاصلهٔ `appliedAtJalali` تا رسیدن به `HIRED` برای time-to-hire) یا در صورت نبودِ منطق، صادقانه `null` برگردانید و UI را برای حالت «ناموجود» آماده کنید.

---

## B4 · متوسط · تولید کد پرسنلی با دامنهٔ محدود و احتمال برخورد (collision)
**فایل:** `server.ts` — خطوط `455` و `1039`

```ts
do {
  personnelCode = toPersianDigits(`10${Math.floor(100 + Math.random() * 900)}`);
} while (existingCodes.has(personnelCode));
```

**شرح:** فقط ۹۰۰ کد ممکن (۱۰۱۰۰ تا ۱۰۹۹۹) تولید می‌شود و کدهای seed موجود (۱۰۰۲۴، ۱۰۱۵۵، ۱۰۲۴۰) در همین دامنه‌اند. با رشد تعداد پرسنل، احتمال برخورد بالا می‌رود و حلقهٔ `while` بدون سقف است (در بدترین حالت بی‌نهایت یا کند). همچنین کدها ترتیبی/قابل‌پیش‌بینی نیستند.

**اصلاح پیشنهادی:** تولید ترتیبی بر اساس بیشینهٔ کد موجود (increment آخرین عدد) یا شمارندهٔ اتمیک؛ و افزودن سقف تکرار برای حلقهٔ تصادفی.

---

## B5 · متوسط · نبود سه endpoint ماژول «حاکمیت هوش مصنوعی» (شکاف قرارداد FE/BE)
**فایل:** `server.ts` (هیچکدام تعریف نشده) در برابر `src/components/ai-governance/AIBotGovernanceModule.tsx`

**Endpointهای فراخوانی‌شده توسط فرانت که در بک‌اند وجود ندارند:**
- `GET /api/ai-governance/config`
- `PUT /api/ai-governance/config`
- `POST /api/ai-governance/test-connection`
- `POST /api/ai-governance/test-evaluate`

**شرح:** ماژول کامل (تنظیم رفتار بات، سند فرهنگ سازمانی، پایپ‌لاین دپارتمان‌ها، تست زنده و sandbox ارزیابی) در فرانت نوشته شده اما بک‌اند آن اصلاً پیاده‌سازی نشده است. هر چهار درخواست `404` می‌گیرند؛ بنابراین «دریافت تنظیمات» با خطا مواجه می‌شود و «تست اتصال» و «ارزیابی sandbox» همیشه شکست می‌خورند.

**اثر:** یک ماژول بزرگ و پرجزئیات که عملاً کار نمی‌کند، در حالی که از Command Palette قابل دسترسی است (به F2 مراجعه کنید).

**اصلاح پیشنهادی:** دو راه‌حل (تصمیم با مالک محصول):
- **(الف)** حذف کامل ماژول از UI (Sidebar + CommandPalette + App) تا زمان پیاده‌سازی بک‌اند؛ یا
- **(ب)** پیاده‌سازی واقعی چهار endpoint در بک‌اند (config در حافظه/اسنپ‌شات، تست اتصال واقعی به Gemini، و ارزیابی با `evaluateCandidateWithCriteria`).

---

# بخش ۲ — باگ‌های فرانت‌اند

## F1 · بالا · جدا شدن state پرسنل در ماژول «پرونده پرسنلی» از state اصلی App
**فایل:** `src/components/employees/EmployeesModule.tsx` (خطوط `78` تا `90`، `222` تا `330`) و `src/App.tsx` (تنها `onCreateEmployee` پاس داده می‌شود)

**شرح:**
- کامپوننت یک `localEmployees` نگه می‌دارد و عملیات **ویرایش (PATCH)** و **حذف (DELETE)** را مستقیماً با `fetch` انجام می‌دهد و فقط `localEmployees` را به‌روز می‌کند.
- پروپ‌های `onUpdateEmployee` و `onDeleteEmployee` در تایپ تعریف شده‌اند اما `App.tsx` آن‌ها را **پاس نمی‌دهد**.
- نتیجه: بعد از ویرایش/حذف یک پرسنل، آرایهٔ `employees` در سطح App **کهنه** می‌ماند و سایر ماژول‌ها (داشبورد، تحلیل‌ها، تردد و مرخصی، پورتال موبایل) تا زمان reload کامل، دادهٔ قدیمی نشان می‌دهند.
- ناهماهنگی منطقی: «ایجاد» (از طریق `onCreateEmployee`) به App برمی‌گردد و sync می‌شود، اما «ویرایش/حذف» نه.

**اثر:** نمایش دادهٔ ناسازگار بین ماژول‌ها (مثلاً پرسنل حذف‌شده هنوز در داشبورد/چارت هست یا حقوق ویرایش‌شده در جای دیگر به‌روز نیست).

**اصلاح پیشنهادی:** افزودن `onUpdateEmployee` و `onDeleteEmployee` در App و به‌روزرسانی state مرکزی `employees`، یا (ساده‌تر) بعد از هر ویرایش/حذف، فراخوانی `fetchData()` برای همگام‌سازی سراسری.

---

## F2 · بالا · ماژول «حاکمیت هوش مصنوعی» از Command Palette قابل دسترسی است ولی Sidebar آن را غیرفعال کرده
**فایل:** `src/components/common/Sidebar.tsx` (آیتم `ai-governance` با `comingSoon: true`) در برابر `src/components/common/CommandPalette.tsx` (خطوط `110` و `207`) و `src/App.tsx` (`selectModule`)

**شرح:**
- در Sidebar آیتم «مدیریت دستیار» با `comingSoon: true` و `badge: 'به‌زودی'` غیرفعال است.
- اما Command Palette دو آیتم `action-ai-governance` و `nav-ai-governance` دارد که مستقیماً `onSelectModule('ai-governance')` صدا می‌زنند.
- فیلتر نقش در Command Palette فقط `recruitment`/`payroll`/`analytics` را گیت می‌کند و `ai-governance` را گیت نمی‌کند؛ `MODULE_ACCESS['ai-governance']` هم شامل `HR_DIRECTOR` و `DEPT_MANAGER` است.
- نتیجه: مدیر HR از Command Palette به ماژولی می‌رسد که بک‌اند ندارد (به B5 مراجعه کنید) و پر از خطا/وضعیت شکست است.

**اصلاح پیشنهادی:** یک منبع واحد حقیقت برای «در دسترس بودن» ماژول بسازید (ترکیب `MODULE_ACCESS` + پرچم `comingSoon`) و در هر سه نقطهٔ Sidebar، Command Palette و `selectModule` اعمال کنید. فعلاً `ai-governance` باید از همهٔ مسیرها غیرقابل دسترسی باشد تا بک‌اندش آماده شود.

---

## F3 · متوسط · نوار پیشرفت بارگذاری گروهی رزومه «جعلی» است
**فایل:** `src/components/recruitment/BulkUploadModal.tsx` — خط `542`

```ts
const next = prev + Math.floor(Math.random() * 12) + 6;
```

**شرح:** پس از کلیک «شروع ارزیابی سرور»، یک `setInterval` هر ۲۵۰ms عدد پیشرفت را با اعداد تصادفی بالا می‌برد (تا سقف ۹۰) و متن مرحله را عوض می‌کند؛ در حالی که پیشرفت واقعی هیچ ارتباطی با آن ندارد (درخواست واقعی `fetch` هم‌زمان در جریان است). متن مرحله هم ادعاهای «ارزیابی در Gemini…» را نمایش می‌دهد حتی وقتی موتور محلی fallback اجرا می‌شود.

**اثر:** UI گمراه‌کننده که پیشرفت ساختگی و ادعای نادرست دربارهٔ منبع ارزیابی نشان می‌دهد.

**اصلاح پیشنهادی:** نوار پیشرفت را به مراحل واقعی گره بزنید (حداقل دو حالت: «در حال ارسال» → «در حال پردازش» → «انجام شد/خطا»)، نه اعداد تصادفی؛ و متن مرحله را با `aiAvailable` واقعی پاسخ سرور هماهنگ کنید.

---

## F4 · متوسط · داده‌های fallback ساختگی و تاریخ‌های hardcoded در تحلیل‌ها/چک‌لیست/سایدبار
**فایل‌ها:**
- `src/components/analytics/AnalyticsModule.tsx` — آرایهٔ `defaultDepts` با اعداد ثابت `48/32/26/18/15/12` روز مرخصی و fallback تاریخ `'۱۴۰۳/۰۶/۱۸'` (خط `240`).
- `src/components/checklists/ChecklistsModule.tsx` — خط `121`: fallback `'۱۴۰۳/۰۶/۱۵'`.
- `src/components/common/Sidebar.tsx` — خط `416`: برچسب `قانون کار ۱۴۰۳`.

**شرح:**
- وقتی دادهٔ واقعی مرخصی کم باشد، «مصرف مرخصی به تفکیک دپارتمان» با اعداد ثابت جعلی پر می‌شود و ظاهر KPI واقعی می‌گیرد.
- تاریخ‌های fallback به سال ۱۴۰۳ اشاره می‌کنند در حالی که سال جاری ۱۴۰۵ است؛ برچسب سایدبار هم دو سال کهنه است.

**اصلاح پیشنهادی:** حذف مقادیر fallback ساختگی و نمایش صادقانهٔ «داده ناکافی»؛ جایگزینی تاریخ‌های hardcoded با `tehranNow`/مقدار واقعی از API؛ به‌روزرسانی برچسب قانون کار به سال جاری (یا حذف عدد سال).

---

## F5 · متوسط · سال پیش‌فرض ماژول حقوق hardcoded روی ۱۴۰۴ است
**فایل:** `src/components/payroll/PayrollModule.tsx` — خط `222`

```ts
const [selectedYear, setSelectedYear] = useState(1404);
```

**شرح:** endpoint `/api/payroll/years` هم `currentYearJalali` (که الان `1405` است) و هم `availableYears` را برمی‌گرداند، اما مقدار اولیهٔ سال روی ۱۴۰۴ hardcode شده. کاربر هنگام باز کردن ماژول حقوق روی یک دورهٔ کهنه می‌افتد و باید دستی سال را جلو ببرد.

**اصلاح پیشنهادی:** بعد از دریافت پاسخ `/api/payroll/years`، `selectedYear` را روی `currentYearJalali` تنظیم کنید (با fallback امن).

---

## F6 · پایین · استفادهٔ ناهماهنگ از `alert()` به‌جای سیستم Toast
**فایل‌ها:**
- `src/components/ai-governance/AIBotGovernanceModule.tsx` (خطوط `169` و `190`)
- `src/components/employees/EmployeeProfileDrawer.tsx` (خط `457`)
- `src/components/recruitment/BulkUploadModal.tsx` (خطوط `372` و `376`)
- `src/components/recruitment/EvaluationCriteriaManager.tsx` (خط `285`)
- `src/components/recruitment/RecruitmentModule.tsx` (خط `167`)

**شرح:** چند نقطه همچنان از `alert()`/`window.confirm` خام استفاده می‌کنند در حالی که بقیهٔ اپ از سیستم Toast/Modal استاندارد استفاده می‌کند. تجربهٔ کاربری ناسازگار و غیرحرفه‌ای.

**اصلاح پیشنهادی:** جایگزینی با `showToast` و (برای تأییدها) مودال تأیید استاندارد موجود.

---

## F7 · پایین · اسناد پرسنلی غیرعملیاتی (fileUrl جعلی `#`)
**فایل:** `src/components/employees/EmployeeProfileDrawer.tsx` — خط `457` و دادهٔ seed در `server/store.ts` (`fileUrl: '#'`)

**شرح:** دکمهٔ مشاهدهٔ سند فقط `alert('امکان مشاهده سند وجود ندارد.')` نشان می‌دهد؛ هیچ جریان واقعی آپلود/نمایش سند وجود ندارد و اسناد seed همگی `fileUrl: '#'` هستند.

**اصلاح پیشنهادی:** یا حذف UI سند تا پیاده‌سازی آپلود واقعی، یا پیاده‌سازی آپلود/ذخیرهٔ سند (تصمیم با مالک محصول).

---

## F8 · پایین · ثبت تردد فقط برای «خودِ» کاربر ممکن است؛ HR نمی‌تواند برای دیگران ثبت کند
**فایل:** `src/App.tsx` (`handleCheckInOut` فقط `{type}` می‌فرستد) و `src/components/attendance/AttendanceModule.tsx` (`onCheckInOut: (type) => void`)

**شرح:** تابع `handleCheckInOut` هیچ `employeeId` نمی‌گیرد؛ بنابراین حتی با نقش `HR_DIRECTOR`، endpoint `/api/attendance/check-in-out` همیشه روی `sessionEmployeeId` (`emp-1`) اعمال می‌شود. در عمل مدیر نمی‌تواند تردد سایر پرسنل را ثبت/اصلاح کند، در حالی که بک‌اند این قابلیت را (برای HR) دارد.

**اثر:** محدودیت عملکردی (نه crash) برای سناریوی ثبت تردد کارکنان توسط HR.

**اصلاح پیشنهادی:** افزودن پارامتر `employeeId` به زنجیرهٔ UI→App→API برای نقش HR، یا شفاف‌سازی UI که این دکمه فقط تردد خود کاربر است.

---

# بخش ۳ — یادداشت‌های معماری (نیازمند تصمیم، نه باگ قطعی)

این موارد عمداً «مستند» شده‌اند و به‌عنوان باگ قطعی لیست نمی‌شوند، اما ارزش طرح در پرامپت را دارند:

1. **نقش کاربری سراسری (singleton)** — `dbStore.currentUserRole` یک مقدار سراسری است نه per-session؛ در استقرار چندکاربرهٔ واقعی باید احراز هویت واقعی جایگزین هدر/سوییچر دمو شود (در README صادقانه ذکر شده).
2. **تقویم تعطیلات رسمی مدل نشده** — محاسبهٔ روز کاری فقط جمعه را حذف می‌کند، تعطیلات رسمی (عید، مناسب‌ها) در نظر گرفته نمی‌شود (در `jalali.ts` مستند شده).
3. **تصمیمات معلق محصول D1–D7** در `PRODUCT_BUG_REPORT.md` (مثلاً مبنای عیدی/سنوات، سال هدف مالیاتی) که هنوز در انتظار تأیید مالک محصول‌اند.
4. **پایداری** — در نبود `DATABASE_URL` فقط اسنپ‌شات JSON در `data/hrms-store.json`؛ اتصال واقعی Supabase برای Employeeها در فاز ۱ انجام شده ولی مابقی موجودیت‌ها همچنان در حافظه‌اند.
